{
  "name": "@perrykit/example-counter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/main.ts",
    "build": "bun build src/main.ts --outdir ./dist",
    "start": "bun run dist/main.js"
  },
  "dependencies": {
    "@perrykit/core": "^0.5.0"
  },
  "devDependencies": {
    "@perrykit/cli": "^0.1.0",
    "bun-types": "latest"
  }
}
